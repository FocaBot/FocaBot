apps:
  - name   : focaBot
    script : main.js
    instances: max
    exec_mode: cluster
    interpreter_args: --harmony-async-await
    env    :
      NODE_ENV: production      
    env_staging:
      NODE_ENV: development
      PORT: 3052
